import { SearchCollapseDirective } from './search-collapse.directive';

describe('SearchCollapseDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchCollapseDirective();
    expect(directive).toBeTruthy();
  });
});
