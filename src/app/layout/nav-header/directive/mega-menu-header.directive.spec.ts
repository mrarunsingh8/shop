import { MegaMenuHeaderDirective } from './mega-menu-header.directive';

describe('MegaMenuHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new MegaMenuHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
